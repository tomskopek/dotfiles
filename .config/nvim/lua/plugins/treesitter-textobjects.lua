return {
  {
    "nvim-treesitter/nvim-treesitter-textobjects",
    -- Pinned alongside nvim-treesitter: same upstream rewrite, same frozen
    -- legacy API. Unpin together with nvim-treesitter when migrating.
    pin = true,
  },
}
